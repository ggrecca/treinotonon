import React from "react";
export function BottomSheet({open, title, children, onClose, className = ""}) { if(!open) return null; return <div className="tt-bottom-sheet-backdrop" role="presentation" onMouseDown={event=>event.target === event.currentTarget && onClose?.()}><section className={`tt-bottom-sheet ${className}`.trim()} role="dialog" aria-modal="true" aria-label={title}><div className="tt-bottom-sheet__handle" aria-hidden="true" />{title && <div className="tt-dialog__header"><h2>{title}</h2>{onClose && <button type="button" className="tt-button tt-button--ghost tt-button--sm" onClick={onClose} aria-label="Fechar">×</button>}</div>}{children}</section></div>; }
